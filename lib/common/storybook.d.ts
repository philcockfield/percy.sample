import * as React from 'react';
import { IHostProps, Story } from 'storybook-host';
export { React };
export { knobs } from 'storybook-host';
export declare function describe(name: string, config: IHostProps): Story;
